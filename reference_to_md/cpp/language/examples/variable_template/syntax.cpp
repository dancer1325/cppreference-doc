// 1. `template < parameter-list > variable-declaration`
template<typename T> first_one;

// 2. `template < parameter-list > requires constraint variable-declaration`
template<typename T> requires TODO: second_one;
